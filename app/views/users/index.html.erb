<table>
	<% @users.each do |user| %>
	<tr>
		<td><%= attachment_image_tag user, :image, :fill, 60, 60, format: 'jpeg', fallback: "no_image.jpg", size:'90x90' %></td>
		<td><%= link_to user.name, user_path(user) %></td>
		<td><%= link_to'SHOW', user_path(user) %></td>
	</tr>
	<% end %>
</table>
